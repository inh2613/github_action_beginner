name :'Basic'

on:
  push:
    branches:
    - main

jobs:
  build-test:
    name: 'Build Test Job'
    runs-on: ubuntu-latest

    steps:
    - name: hello world
      shell: bash
      run: |
        echo "hello world"
  dev:
    name: 'Dev Job'
    needs:
    - build-test
    runs-on: ubuntu-latest

    steps:
    - name: hello world
      shell: bash
      run: |
        echo "hello world"